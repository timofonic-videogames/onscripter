* TO BUILD ONSCRIPTER FOR YOUR USE

This is the usual

  ./configure
  make
  sudo make install


* TO BUILD A REDISTRIBUTABLE ONSCRIPTER BINARY

  ./configure --with-internal-libs
  make


* NOTES

For Windows builds, use the MinGW compilers in the MSYS environment.
